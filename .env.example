# ─────────────────────────────────────────────────────────────
#  MediCare Plus — Environment Variables
#  Copy this file to .env for local development.
#  Set these same variables in Railway → Variables tab for production.
#  NEVER commit the real .env file to Git.
# ─────────────────────────────────────────────────────────────

# REQUIRED: Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-this-with-a-long-random-string

# Admin account created on first run
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@yourhospital.com
ADMIN_PASSWORD=ChangeThisStrongPassword123!

# On Railway with a Volume, set this to /data/hospital.db
# Leave blank for local dev (uses ./hospital.db)
DB_PATH=

# Set to "production" on Railway
FLASK_ENV=production

# Hospital branding (appears in emails and page header)
HOSPITAL_NAME=MediCare Plus Hospital
HOSPITAL_PHONE=+1-800-MEDICARE
